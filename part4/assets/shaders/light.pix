in vec3 _color;
in vec3 _spacePos;

void main()
{
  gl_FragData[0] = vec4(_spacePos, 1.0f); // space position
  gl_FragData[1] = vec4(_color.rgb, 1.0f); // diffuse
  gl_FragData[2] = vec4(_spacePos, 1.0f);
  gl_FragData[3] = vec4(_spacePos, 0.0f); // tex coord
}
